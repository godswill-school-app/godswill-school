name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
      - name: Create Android Project Files
        run: |
          mkdir -p app/src/main/java/com/godswill/school
          cp MainActivity.kt app/src/main/java/com/godswill/school/ 2>/dev/null || true
          echo 'include ":app"' > settings.gradle
          echo 'plugins { id "com.android.application" version "8.1.4" apply false; id "org.jetbrains.kotlin.android" version "1.9.10" apply false }' > build.gradle
          cat > app/build.gradle <<'EOL'
          plugins { id "com.android.application"; id "org.jetbrains.kotlin.android" }
          android { namespace "com.godswill.school"; compileSdk 34; defaultConfig { applicationId "com.godswill.school"; minSdk 24; targetSdk 34; versionCode 1; versionName "1.0" } buildTypes { release { minifyEnabled false } } }
          dependencies { implementation "androidx.core:core-ktx:1.12.0"; implementation "androidx.compose.ui:ui:1.5.4"; implementation "androidx.compose.material3:material3:1.1.2"; implementation "androidx.activity:activity-compose:1.8.2" }
          EOL
          mkdir -p app/src/main
          echo '<manifest xmlns:android="http://schemas.android.com/apk/res/android"><application android:label="Gods Will School"><activity android:name=".MainActivity" android:exported="true"><intent-filter><action android:name="android.intent.action.MAIN"/><category android:name="android.intent.category.LAUNCHER"/></intent-filter></activity></application></manifest>' > app/src/main/AndroidManifest.xml
      - name: Build APK
        uses: gradle/gradle-build-action@v3
        with:
          gradle-version: 8.5
          arguments: assembleDebug
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: GodsWill-School-App
          path: app/build/outputs/apk/debug/app-debug.apk
